# DB connection
DB_HOST=localhost         # or 127.0.0.1 (Docker: host.docker.internal or container name)
DB_PORT=3306              # MySQL TCP port
DB_USER=root          # or root
DB_PASSWORD=Sai@0714
DB_NAME=bmw_grid

# App
PORT=4000                 # Express server port
